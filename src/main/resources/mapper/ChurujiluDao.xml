<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.ChurujiluDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.ChurujiluEntity" id="churujiluMap">
        <result property="guanlizhanghao" column="guanlizhanghao"/>
        <result property="guanlixingming" column="guanlixingming"/>
        <result property="xiaoqumingcheng" column="xiaoqumingcheng"/>
        <result property="waimaizhanghao" column="waimaizhanghao"/>
        <result property="waimaixingming" column="waimaixingming"/>
        <result property="lianxidianhua" column="lianxidianhua"/>
        <result property="peisongpingtai" column="peisongpingtai"/>
        <result property="jinrushijian" column="jinrushijian"/>
        <result property="likaishijian" column="likaishijian"/>
        <result property="churuzhuangtai" column="churuzhuangtai"/>
        <result property="dengjiriqi" column="dengjiriqi"/>
        <result property="crossuserid" column="crossuserid"/>
        <result property="crossrefid" column="crossrefid"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.ChurujiluVO" >
		SELECT * FROM churujilu  churujilu         
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.ChurujiluVO" >
		SELECT  churujilu.* FROM churujilu  churujilu 	
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.ChurujiluView" >

		SELECT  churujilu.* FROM churujilu  churujilu 	        
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.ChurujiluView" >
		SELECT * FROM churujilu  churujilu <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	

    <select id="selectValue" resultType="map" >
        SELECT ${params.xColumn}, ROUND(sum(${params.yColumn}),1) total FROM churujilu
        <where> 1=1 ${ew.sqlSegment}</where>
        group by ${params.xColumn}
    </select>

    <select id="selectTimeStatValue" resultType="map" >
        <if test = 'params.timeStatType == "日"'>
                SELECT DATE_FORMAT(${params.xColumn},'%Y-%m-%d') ${params.xColumn}, sum(${params.yColumn}) total FROM churujilu
                <where> 1=1 ${ew.sqlSegment}</where>
                group by DATE_FORMAT(${params.xColumn},'%Y-%m-%d')
        </if>
        <if test = 'params.timeStatType == "月"'>
                SELECT DATE_FORMAT(${params.xColumn},'%Y-%m') ${params.xColumn}, sum(${params.yColumn}) total FROM churujilu
                <where> 1=1 ${ew.sqlSegment}</where>
                group by DATE_FORMAT(${params.xColumn},'%Y-%m')
        </if>
        <if test = 'params.timeStatType == "年"'>
                SELECT DATE_FORMAT(${params.xColumn},'%Y') ${params.xColumn}, sum(${params.yColumn}) total FROM churujilu
                <where> 1=1 ${ew.sqlSegment}</where>
                group by DATE_FORMAT(${params.xColumn},'%Y')
        </if>
    </select>

    <select id="selectGroup" resultType="map" >
        SELECT ${params.column} , count(1) total FROM churujilu
        <where> 1=1 ${ew.sqlSegment}</where>
        group by ${params.column}
    </select>




</mapper>
